[project]
name = "mcbackup"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcrcon"]

[project.scripts]
mcbackup = "mcbackup.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"